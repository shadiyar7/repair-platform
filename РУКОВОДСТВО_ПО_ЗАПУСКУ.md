# Руководство по запуску платформы Re:Pair (DYNAMIX)

Это руководство поможет вам запустить проект локально на вашем компьютере без необходимости каждый раз обращаться к ИИ.

## 📋 Предварительные требования

Убедитесь, что у вас установлены:
- **Node.js**: версии 16 или выше (у вас установлена v16.17.0)
- **Ruby**: версии 3.2 (у вас установлена 3.2.0)
- **Bundler**: `gem install bundler`

## 🚀 Быстрый запуск

Самый простой способ запустить всё сразу — использовать готовый скрипт:

1. Откройте терминал в папке проекта.
2. Запустите скрипт:
   ```bash
   ./start-all.sh
   ```

*Если скрипт не запускается, дайте ему права: `chmod +x start-all.sh`*

---

## 🛠 Ручной запуск (по отдельности)

Если вы хотите запустить компоненты в разных окнах терминала:

### 1. Бэкенд (Ruby on Rails)
```bash
cd backend
bundle install         # Установка зависимостей (только при первом запуске или изменениях)
bin/rails db:setup     # Настройка базы (только при первом запуске)
bin/rails s            # Запуск сервера
```
Сервер будет доступен по адресу: `http://localhost:3000`

### 2. Фронтенд (React + Vite)
```bash
cd frontend
npm install            # Установка зависимостей (только при первом запуске)
npm run dev            # Запуск сервера разработки
```
Интерфейс будет доступен по адресу: `http://localhost:5173`

---

## 🔑 Тестовые данные для входа

В системе уже созданы пользователи для тестов:

- **Админ**: `admin@repair.com` / `password123`
- **Склад (Менеджер)**: `warehouse@repair.com` / `password123`
- **Клиент**: `client@repair.com` / `password123`
- **Водитель**: `driver@repair.com` / `password123`

---

## ❓ Решение проблем

- **Конфликт портов**: Если порт 3000 или 5173 занят, проверьте другие запущенные процессы.
- **Ошибка базы данных**: Если бэкенд ругается на базу, попробуйте `bin/rails db:migrate` или `bin/rails db:reset`.
- **Node_modules**: Если фронтенд ведет себя странно, удалите папку `node_modules` и выполните `npm install` заново.
